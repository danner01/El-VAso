enum LoginState {
  cheking,
  success,
  loading,
  error,
  noAuth,
  checking,
}

enum RegisterState {
  cheking,
  success,
  loading,
  error,
  noAuth,
}

enum RegisterSocialState {
  cheking,
  success,
  loading,
  error,
  noAuth,
}

enum VerifyState {
  cheking,
  success,
  loading,
  error,
  noAuth,
}

enum VerifyRecoveryState {
  cheking,
  success,
  loading,
  error,
  noAuth,
}

enum ResendState {
  cheking,
  success,
  loading,
  error,
  noAuth,
}
